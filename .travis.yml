language: node_js
node_js: node
cache:
  directories:
    - node_modules
script:
  - npm test
env:
  - CI=true

before_deploy:
  - npm install netlify-cli -g
  - npm run build

deploy:
  provider: script
  script: netlify deploy -s cameron-speed -t db560dd38047421bec8a4bc075d80ef980ae421a7926eb63a56cea64da00b34c -p ./build
  skip_cleanup: true
  on:
    branch: mas